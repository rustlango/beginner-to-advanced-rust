// lecture notes on traits

// Traits 
// In other languages, we call Traits interfaces. 

// A trait (interface) is a promise that the struct (class) on which the
// trait is specified, implements the trait. Without implementation, 
// the code fails to compile.


