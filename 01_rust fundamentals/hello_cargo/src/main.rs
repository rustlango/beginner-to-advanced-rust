fn main() {
    println!("Hello, cargo!");
}

// Cargo

// rust package management system
// To create a new project: cargo new projectname
// Cargo creates a new folder with gitignore and lock. Cargo.toml is like package.json in an npm project (package management, dependencies)
// cargo build:builds the code. It creates a target folder. Inside target/debug, hello.exe is created.
// cargo run: builds and runs the code 